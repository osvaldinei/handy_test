name: handy_test
description: Projeto Avaliativo handyhostel.


publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.12.2
  google_fonts: ^1.1.1
  cupertino_icons: ^1.0.0
  

  flutter_localizations:
    sdk: flutter
flutter_icons:
  ios: true
  android: true
  image_path_ios: "assets/launcher/icon.png"
  image_path_android: "assets/launcher/icon.png"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.8.1  
flutter:
  uses-material-design: true

  assets:
    - assets/logo.png